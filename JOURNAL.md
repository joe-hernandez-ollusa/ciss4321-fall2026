# Engineering Journal — Jose T. Hernandez

While setting up my local repository, my git push commands kept failing with a remote repository error. I realized I had misspelled the remote alias as "orgin" instead of "origin". Correcting the spelling and re-initializing my local repository completely fixed the connection issue.